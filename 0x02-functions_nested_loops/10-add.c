#include "main.h"
/**
 * * *add - a function that adds two integers
 * * returns result
 * * *Returns: Always 0 (success)
 * **/
int add()
{
	int a;
	int b;

	n = a+b;
	printf("%d + %d = %d\n", a,b,n);
	return (0);
}

