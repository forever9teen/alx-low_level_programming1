#include "main.h"
/**
 * * * int _abs - a function that computes the absolute value of an integer.
 * * @n: parameter
 * Return: absolute value of a number
 * **/
int _abs(int)
{
	if ( n > 0)
		n = -n;
	return (n);
}
