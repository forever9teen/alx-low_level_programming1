#include "main.h"
/**
 * * * int _abs - prints  the absolute value of an integer.
 * * @n: parameter
 * * *Return: absolute value of an integer.
 * **/
int _abs(int)
{
	if (n < 0)
		n = -n;
	return (n);
}
